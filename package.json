{
  "name": "ayazmo-plugin-template",
  "version": "0.1.0",
  "description": "Ayazmo plugin template",
  "main": "./dist/index.js",
  "author": "ayazmojs",
  "license": "MIT",
  "rootDir": "src",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1",
    "@ayazmo/types": "^0.1.0"
  },
  "scripts": {
    "build": "cross-env && tsc -p tsconfig.json"
  }
}
